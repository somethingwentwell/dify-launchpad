# Development environment configuration
NEXT_PUBLIC_DEPLOY_ENV=DEVELOPMENT
NEXT_PUBLIC_EDITION=SELF_HOSTED

# API endpoints - you'll need to replace these with your actual Dify backend URLs
NEXT_PUBLIC_API_PREFIX=http://localhost/console/api
NEXT_PUBLIC_PUBLIC_API_PREFIX=http://localhost/api

# The base URL for published Dify apps (where users access the published apps)
# This should be your main Dify domain, not the launchpad domain
NEXT_PUBLIC_APP_BASE_URL=http://localhost

# For development without backend, you can use a mock service
# NEXT_PUBLIC_API_PREFIX=http://localhost:3001/mock/console/api
# NEXT_PUBLIC_PUBLIC_API_PREFIX=http://localhost:3001/mock/api

NEXT_PUBLIC_MARKETPLACE_API_PREFIX=https://marketplace.dify.ai/api/v1
NEXT_PUBLIC_MARKETPLACE_URL_PREFIX=https://marketplace.dify.ai

# Disable telemetry
NEXT_TELEMETRY_DISABLED=1

# Other settings
NEXT_PUBLIC_TEXT_GENERATION_TIMEOUT_MS=60000
NEXT_PUBLIC_UPLOAD_IMAGE_AS_ICON=false
NEXT_PUBLIC_ALLOW_UNSAFE_DATA_SCHEME=false
NEXT_PUBLIC_TOP_K_MAX_VALUE=10
NEXT_PUBLIC_INDEXING_MAX_SEGMENTATION_TOKENS_LENGTH=4000
NEXT_PUBLIC_LOOP_NODE_MAX_COUNT=100
NEXT_PUBLIC_MAX_TOOLS_NUM=10
NEXT_PUBLIC_MAX_PARALLEL_LIMIT=10
NEXT_PUBLIC_MAX_ITERATIONS_NUM=99
NEXT_PUBLIC_MAX_TREE_DEPTH=50

# Website crawling
NEXT_PUBLIC_ENABLE_WEBSITE_JINAREADER=true
NEXT_PUBLIC_ENABLE_WEBSITE_FIRECRAWL=true
NEXT_PUBLIC_ENABLE_WEBSITE_WATERCRAWL=true